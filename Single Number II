class Solution {
public:
    int singleNumber(vector<int>& nums) {
        int ones = 0, tows = 0;
        int len = nums.size();
        for(int i = 0; i < len; i++) {
            ones = (ones ^ nums[i]) & ~tows;
            tows = (tows ^ nums[i]) & ~ones;
        }
        return ones;
    }
};